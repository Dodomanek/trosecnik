--- a/src/game_logic.js
+++ b/src/game_logic.js
@@ -552,47 +575,52 @@
  if(pane&&!mini)closePanel();
  changed=true;save(true);updateUI();flash('↩ Robinson je zpátky na bezpečném místě.');
 }
-function moveHero(dx,dz,dt,sprint){let survivalPace=(state.water<20?.78:1)*(state.food<18?.86:1)*(state.energy<16?.80:1)*(state.morale<25?.88:1);let speed=(sprint&&state.energy>7?4.7:2.55)*(state.skills.scout?1.12:1)*(state.skills.masterExplorer&&Math.hypot(hero.x+4,hero.z+2)>9?1.08:1)*movementLoadFactor(state.bag)*survivalPace,mag=Math.hypot(dx,dz);if(mag<.09||pending||pane){hero.walk=mix(hero.walk,0,clamp(dt*8,0,1));return false}dx/=mag;dz/=mag;speed*=clamp(1.3-Math.hypot(H(hero.x+.30,hero.z)-H(hero.x-.30,hero.z),H(hero.x,hero.z+.30)-H(hero.x,hero.z-.30))*.3,.72,1.12);let step=dt*speed,nx=hero.x+dx*step,nz=hero.z+dz*step,success=false;
+function moveHero(dx,dz,dt,sprint){let survivalPace=(state.water<20?.78:1)*(state.food<18?.86:1)*(state.energy<16?.80:1)*(state.morale<25?.88:1)*(state.injuries.fracture>0?.72:1);let speed=(crouching?1.65:(sprint&&state.energy>7?4.7:2.55))*(state.skills.scout?1.12:1)*(state.skills.masterExplorer&&Math.hypot(hero.x+4,hero.z+2)>9?1.08:1)*movementLoadFactor(state.bag)*survivalPace,mag=Math.hypot(dx,dz);if(mag<.09||pending||pane){hero.walk=mix(hero.walk,0,clamp(dt*8,0,1));return false}dx/=mag;dz/=mag;speed*=clamp(1.3-Math.hypot(H(hero.x+.30,hero.z)-H(hero.x-.30,hero.z),H(hero.x,hero.z+.30)-H(hero.x,hero.z-.30))*.3,.72,1.12);let step=dt*speed,nx=hero.x+dx*step,nz=hero.z+dz*step,success=false;
 if(passable(nx,nz)){hero.x=nx;hero.z=nz;success=true}else if(passable(nx,hero.z)){hero.x=nx;success=true}else if(passable(hero.x,nz)){hero.z=nz;success=true}
-if(success){hero.y=H(hero.x,hero.z);hero.yaw+=Math.atan2(Math.sin(facing(dx,dz)-hero.yaw),Math.cos(facing(dx,dz)-hero.y))*clamp(dt*11,0,1);hero.anim+=dt*(sprint?1.5:1.0)*2.5;hero.walk=mix(hero.walk,sprint?1.3:1,clamp(dt*8,0,1));state.x=hero.x;state.z=hero.z;state.yaw=hero.yaw;checkpointHero();state.energy=clamp(state.energy-dt*(sprint?(state.skills.endurance?.72:1.12):.22),0,100);let surv=state.skills.survivor?.76:1;state.food=clamp(state.food-dt*.012*surv,0,100);state.water=clamp(state.water-dt*(Math.hypot(hero.x,hero.z)>29?.082:.026)*surv,0,100);state.food=clamp(state.food-dt*(Math.hypot(hero.x,hero.z)>29?.038:0)*surv,0,100);changed=true;return true}hero.walk=mix(hero.walk,0,clamp(dt*7,0,1));return false}
+if(success){hero.y=H(hero.x,hero.z);hero.yaw+=Math.atan2(Math.sin(facing(dx,dz)-hero.yaw),Math.cos(facing(dx,dz)-hero.y))*clamp(dt*11,0,1);hero.anim+=dt*(sprint?1.5:1.0)*2.5;hero.walk=mix(hero.walk,sprint?1.3:1,clamp(dt*8,0,1));state.x=hero.x;state.z=hero.z;state.yaw=hero.yaw;checkpointHero();state.energy=clamp(state.energy-dt*(crouching?.12:sprint?(state.skills.endurance?.72:1.12):.22),0,100);let stepNoise=crouching?10:(sprint?72:28);if(state.skills.silentStep)stepNoise*=.45;state.noise=clamp(Math.max(state.noise,stepNoise),0,100);let surv=state.skills.survivor?.76:1;state.food=clamp(state.food-dt*.012*surv,0,100);state.water=clamp(state.water-dt*(Math.hypot(hero.x,hero.z)>29?.082:.026)*surv,0,100);state.food=clamp(state.food-dt*(Math.hypot(hero.x,hero.z)>29?.038:0)*surv,0,100);changed=true;return true}hero.walk=mix(hero.walk,0,clamp(dt*7,0,1));return false}
 function updateCamera(dt){if(firstPerson){let h=H(hero.x,hero.z)+1.70,cp=Math.cos(cameraPitch),focus=[hero.x-Math.sin(cameraYaw)*cp*5,h-Math.sin(cameraPitch)*5,hero.z-Math.cos(cameraYaw)*cp*5];eye=[hero.x,h,hero.z];cameraPos=eye.slice();VP=mul(perspective(74*Math.PI/180,Math.max(.1,$('world').clientWidth/$('world').clientHeight),.055,190),lookAt(eye,focus));inverseVP=inverse(VP);return}let distance=shoulder?4.0:wideDistance;let pitch=shoulder?clamp(cameraPitch,.05,.65):cameraPitch;if(camGround===null)camGround=H(hero.x,hero.z);camGround=mix(camGround,H(hero.x,hero.z),clamp(dt*3.6,0,1));let focus=[hero.x+(shoulder?Math.cos(hero.yaw)*.34:0),camGround+1.43,hero.z-(shoulder?Math.sin(hero.yaw)*.34:0)];let dir=[Math.sin(cameraYaw)*Math.cos(pitch),Math.sin(pitch),Math.cos(cameraYaw)*Math.cos(pitch)];let desired=[focus[0]+dir[0]*distance,focus[1]+1.2+dir[1]*distance,focus[2]+dir[2]*distance];
 // Camera line-of-sight: pull in before static obstacles; never burrow through ground.
 for(let t=.30;t<=1;t+=.07){let p=[mix(focus[0],desired[0],t),mix(focus[1],desired[1],t),mix(focus[2],desired[2],t)];let occluded=p[1]<H(p[0],p[2])+.72;for(let tree of staticTrees)if(Math.hypot(p[0]-tree.x,p[2]-tree.z)<tree.r&&p[1]<H(tree.x,tree.z)+3.3)occluded=true;if(occluded){let k=Math.max(.23,t-.14);desired=[mix(focus[0],desired[0],k),mix(focus[1],desired[1],k),mix(focus[2],desired[2],k)];break}}
 desired[1]=Math.max(desired[1],H(desired[0],desired[2])+.75,-.1);let sm=clamp(dt*8,0,1);for(let j=0;j<3;j++)cameraPos[j]=mix(cameraPos[j],desired[j],sm);eye=cameraPos.slice();VP=mul(perspective((shoulder?62:68)*Math.PI/180,Math.max(.1,$('world').clientWidth/$('world').clientHeight),.09,190),lookAt(eye,focus));inverseVP=inverse(VP)}
 function siteDurability(site){return clamp(Number(state.durability?.[site]??100),0,100)}
-function campComfort(){let score=0;if(state.built.shelter===3&&siteDurability('shelter')>=40)score+=2;if(state.built.storage===3&&siteDurability('storage')>=40)score+=1;if(state.built.filter===3&&siteDurability('filter')>=40)score+=1;if(state.built.rainCollector===3&&siteDurability('rainCollector')>=40)score+=1;if(state.built.dryRack===3&&siteDurability('dryRack')>=40)score+=1;if(state.built.fire===3&&state.fireFuel>0)score+=2;return score}
-function processStormDamage(){if(state.weather!=='storm'||state.stormDamageDay===state.day)return;state.stormDamageDay=state.day;const sites=['shelter','storage','filter','rainCollector','trapLine','dryRack'].filter(id=>state.built[id]===3);if(!sites.length)return;const site=sites[(state.day+state.deathCount)%sites.length];const dmg=18+(state.day*7)%17;state.durability[site]=clamp(siteDurability(site)-dmg,0,100);state.fireFuel=clamp(state.fireFuel-18,0,100);writeStory('Bouře poškodila '+BUILD[site].name.toLowerCase()+' na '+Math.round(state.durability[site])+' % odolnosti.');flash('⛈ Bouře poškodila '+BUILD[site].name+' · '+Math.round(state.durability[site])+' %');changed=true}
-function processCampRaid(){if(state.day<=state.campRaidDay)return;state.campRaidDay=state.day;if(state.day<3||state.day%2!==0)return;const defense=(state.built.trapLine===3&&siteDurability('trapLine')>=40?1:0)+(state.built.fire===3&&state.fireFuel>20?1:0)+(state.built.storage===3&&siteDurability('storage')>=40?1:0);if(defense>=2){state.morale=clamp(state.morale+2,0,100);writeStory('Noční šramot kolem tábora utichl. Oheň a pasti odradily zvířata.');return;}const targets=['smokedMeat','meat','fish','coconut','trailSnack'];let stolen=null,from=null;for(const id of targets){if((state.storage?.[id]||0)>0){state.storage[id]--;stolen=id;from='skladu';break}if((state.bag?.[id]||0)>0){state.bag[id]--;stolen=id;from='batohu';break}}if(stolen){state.morale=clamp(state.morale-7,0,100);if(state.built.storage===3)state.durability.storage=clamp(siteDurability('storage')-10,0,100);writeStory('V noci se do tábora dostala zvěř a odnesla '+ITEM[stolen][1].toLowerCase()+' z '+from+'.');flash('🌙 Noční nájezd: ztraceno '+ITEM[stolen][1]);changed=true}}
-function processSurvivalDay(){if(state.day<=state.lastSurvivalRewardDay)return;const healthy=state.hp>=45&&state.water>=20&&state.food>=20;if(healthy){state.survivalStreak=(state.survivalStreak||0)+1;const xp=Math.min(55,12+state.survivalStreak*4);awardXP(xp,'Přežitý den · série '+state.survivalStreak);state.morale=clamp(state.morale+8,0,100);state.journal.push('Den '+state.day+': Přežil jsem další den. Série přežití: '+state.survivalStreak+'.');}else{state.survivalStreak=0;state.morale=clamp(state.morale-8,0,100);}state.lastSurvivalRewardDay=state.day;processCampRaid();changed=true}
-function gameplay(dt){if(!started||pane||mini)return;state.seconds+=dt;updateNPCs(dt);updateExploration();state.clock+=dt/120;if(Math.floor(state.seconds/5)!==Math.floor((state.seconds-dt)/5))changed=true; if(state.clock>=24){state.clock-=24;state.day++;changed=true}processSurvivalDay();if(state.story.stormUntil&&state.seconds<state.story.stormUntil)state.weather='storm';else if(state.day%5===0&&state.clock>10&&state.clock<15)state.weather='heat';else if(state.day%4===0&&state.clock>5&&state.clock<9)state.weather='fog';else if(state.day%3===0&&state.clock>13&&state.clock<17)state.weather='rain';else state.weather='clear';processStormDamage();if((state.bag.meat||0)>0&&!state.meatSpoilAt)state.meatSpoilAt=state.seconds+240;if((state.bag.meat||0)>0&&state.meatSpoilAt&&state.seconds>=state.meatSpoilAt){const spoiled=state.bag.meat;state.bag.meat=0;state.bag.spoiledMeat=(state.bag.spoiledMeat||0)+spoiled;state.meatSpoilAt=0;flash('🤢 Syrové maso se zkazilo.');writeStory('Maso bez zpracování nevydrželo tropické vedro.');}if(state.built.fire===3&&state.fireFuel>0)state.fireFuel=clamp(state.fireFuel-dt*(state.weather==='storm'?.11:.045),0,100);if(state.built.rainCollector===3&&(state.weather==='rain'||state.weather==='storm'))state.rainWater=clamp(state.rainWater+dt*(state.weather==='storm'?.34:.20),0,100);
+function campComfort(){let score=0;if(state.built.shelter===3&&siteDurability('shelter')>=40)score+=2;if(state.built.storage===3&&siteDurability('storage')>=40)score+=1;if(state.built.filter===3&&siteDurability('filter')>=40)score+=1;if(state.built.rainCollector===3&&siteDurability('rainCollector')>=40)score+=1;if(state.built.dryRack===3&&siteDurability('dryRack')>=40)score+=1;if(state.built.watchtower===3&&siteDurability('watchtower')>=40)score+=1;if(state.built.fire===3&&state.fireFuel>0)score+=2;return score}
+function npcTrust(id){return clamp(Number(state.story?.trust?.[id])||0,0,100)}
+function campDefense(){return (state.built.trapLine===3&&siteDurability('trapLine')>=40?1:0)+(state.built.fire===3&&state.fireFuel>20?1:0)+(state.built.storage===3&&siteDurability('storage')>=40?1:0)+(state.built.fence===3&&siteDurability('fence')>=40?2:0)+(state.built.watchtower===3&&siteDurability('watchtower')>=40?2:0)+(npcTrust('mara')>=45?1:0)}
+function processStormDamage(){if(state.weather!=='storm'||state.stormDamageDay===state.day)return;state.stormDamageDay=state.day;const sites=['shelter','storage','filter','rainCollector','trapLine','dryRack','workbench','fence','watchtower','raft'].filter(id=>state.built[id]===3);if(!sites.length)return;const site=sites[(state.day+state.deathCount)%sites.length];const dmg=18+(state.day*7)%17;state.durability[site]=clamp(siteDurability(site)-dmg,0,100);state.fireFuel=clamp(state.fireFuel-18,0,100);writeStory('Bouře poškodila '+BUILD[site].name.toLowerCase()+' na '+Math.round(state.durability[site])+' % odolnosti.');flash('⛈ Bouře poškodila '+BUILD[site].name+' · '+Math.round(state.durability[site])+' %');changed=true}
+function processCampRaid(){if(state.day<=state.campRaidDay)return;state.campRaidDay=state.day;if(state.day<3||state.day%2!==0)return;const defense=campDefense();if(defense>=3){state.morale=clamp(state.morale+2,0,100);writeStory('Noční šramot kolem tábora utichl. Oheň a pasti odradily zvířata.');return;}const targets=['smokedMeat','meat','fish','coconut','trailSnack'];let stolen=null,from=null;for(const id of targets){if((state.storage?.[id]||0)>0){state.storage[id]--;stolen=id;from='skladu';break}if((state.bag?.[id]||0)>0){state.bag[id]--;stolen=id;from='batohu';break}}if(stolen){state.morale=clamp(state.morale-7,0,100);if(state.built.storage===3)state.durability.storage=clamp(siteDurability('storage')-10,0,100);writeStory('V noci se do tábora dostala zvěř a odnesla '+ITEM[stolen][1].toLowerCase()+' z '+from+'.');flash('🌙 Noční nájezd: ztraceno '+ITEM[stolen][1]);changed=true}}
+function processDynamicEvent(){if(state.day<=state.dynamicEventDay)return;state.dynamicEventDay=state.day;if(state.day<4)return;const type=['drift','predator','radio','calm'][state.day%4];state.activeEvent=type;if(type==='drift'){add('wood',1);add('fiber',1);writeStory('Příliv vyhodil na vzdálenou pláž čerstvé trosky. Dynamická událost přidala materiál.');flash('🌊 Událost: příliv přinesl trosky.');}else if(state.built.watchtower===3&&siteDurability('watchtower')>=40)state.morale=clamp(state.morale+2,0,100);if(type==='predator'){state.morale=clamp(state.morale-4,0,100);state.noise=clamp(state.noise+25,0,100);writeStory('Kolem tábora se objevily nové stopy predátorů. Opevnění bude důležitější.');flash('🐾 Událost: nové stopy kolem tábora.');}else if(type==='radio'){const bonus=npcTrust('ivo')>=45?2:1;add('scrap',bonus);state.morale=clamp(state.morale+4,0,100);writeStory('Krátký rádiový šum odhalil kovové trosky na pobřeží.');flash('📻 Událost: rádiový záchyt a kovové trosky.');}else{state.morale=clamp(state.morale+6,0,100);state.flags.calmSeaDay=state.day;writeStory('Moře je nezvykle klidné. Dnešní plavba na voru bude levnější.');flash('🌤 Událost: klidné moře.');}changed=true}
+function processSurvivalDay(){if(state.day<=state.lastSurvivalRewardDay)return;const healthy=state.hp>=45&&state.water>=20&&state.food>=20;if(healthy){state.survivalStreak=(state.survivalStreak||0)+1;const xp=Math.min(55,12+state.survivalStreak*4);awardXP(xp,'Přežitý den · série '+state.survivalStreak);state.morale=clamp(state.morale+8,0,100);state.journal.push('Den '+state.day+': Přežil jsem další den. Série přežití: '+state.survivalStreak+'.');}else{state.survivalStreak=0;state.morale=clamp(state.morale-8,0,100);}state.lastSurvivalRewardDay=state.day;processCampRaid();processDynamicEvent();changed=true}
+function gameplay(dt){if(!started||pane||mini)return;state.seconds+=dt;state.noise=clamp(state.noise-dt*18,0,100);state.crouched=crouching;updateNPCs(dt);updateExploration();state.clock+=dt/120;if(Math.floor(state.seconds/5)!==Math.floor((state.seconds-dt)/5))changed=true; if(state.clock>=24){state.clock-=24;state.day++;changed=true}processSurvivalDay();if(state.story.stormUntil&&state.seconds<state.story.stormUntil)state.weather='storm';else if(state.day%5===0&&state.clock>10&&state.clock<15)state.weather='heat';else if(state.day%4===0&&state.clock>5&&state.clock<9)state.weather='fog';else if(state.day%3===0&&state.clock>13&&state.clock<17)state.weather='rain';else state.weather='clear';processStormDamage();if((state.bag.meat||0)>0&&!state.meatSpoilAt)state.meatSpoilAt=state.seconds+240;if((state.bag.meat||0)>0&&state.meatSpoilAt&&state.seconds>=state.meatSpoilAt){const spoiled=state.bag.meat;state.bag.meat=0;state.bag.spoiledMeat=(state.bag.spoiledMeat||0)+spoiled;state.meatSpoilAt=0;flash('🤢 Syrové maso se zkazilo.');writeStory('Maso bez zpracování nevydrželo tropické vedro.');}if(state.built.fire===3&&state.fireFuel>0)state.fireFuel=clamp(state.fireFuel-dt*(state.weather==='storm'?.11:.045),0,100);if(state.built.rainCollector===3&&(state.weather==='rain'||state.weather==='storm'))state.rainWater=clamp(state.rainWater+dt*(state.weather==='storm'?.34:.20),0,100);
 let forward=(keys.has('w')||keys.has('ArrowUp')?1:0)-(keys.has('s')||keys.has('ArrowDown')?1:0)-joy.y;let strafe=(keys.has('d')||keys.has('ArrowRight')?1:0)-(keys.has('a')||keys.has('ArrowLeft')?1:0)+joy.x;let dx=strafe*Math.cos(cameraYaw)-forward*Math.sin(cameraYaw),dz=-strafe*Math.sin(cameraYaw)-forward*Math.cos(cameraYaw);
 if(Math.hypot(dx,dz)>.09){autogoal=null;hero.goal=null;moveHero(dx,dz,dt,running||keys.has('Shift'))}else if(autogoal){let dd=d2(hero,autogoal);if(dd<Math.max(1.15,autogoal.r+.80)){let goal=autogoal;autogoal=null;if(goal.o)beginInteraction(goal.o);hero.walk=mix(hero.walk,0,dt*9)}else {let stepX=(autogoal.x-hero.x)/dd,stepZ=(autogoal.z-hero.z)/dd;let moved=moveHero(stepX,stepZ,dt,true);if(!moved&&hero.walk<.13)autogoal=null}}else{hero.walk=mix(hero.walk,0,clamp(dt*6,0,1));state.energy=clamp(state.energy+dt*(state.built.shelter===3?.80:.48),0,100)}
 if(state.food<=0||state.water<=0){state.hp=clamp(state.hp-dt*.44,0,100);if(state.hp<=0)handleDeath(state.water<=0?'dehydratace':'hlad')}
 updateWildSurvival(dt);
-if(pending){pending.remaining-=dt;hero.working=1-pending.remaining/pending.duration;hero.kind=pending.kind;if(pending.remaining<=0){let w=pending.work;pending=null;hero.working=0;try{w()}catch(e){console.error(e);flash('Akci se nepodařilo dokončit.')}}}else hero.working=0;
+if(pending){pending.remaining-=dt;hero.working=1-pending.remaining/pending.duration;hero.kind=pending.kind;state.noise=clamp(Math.max(state.noise,pending.kind==='chop'||pending.kind==='build'?78:pending.kind==='fight'||pending.kind==='thrust'?88:38),0,100);if(pending.remaining<=0){let w=pending.work;pending=null;hero.working=0;try{w()}catch(e){console.error(e);flash('Akci se nepodařilo dokončit.')}}}else hero.working=0;
 if(now()-lastHud>160){lastHud=now();updateUI()}}
 function updateWildSurvival(dt){
  if(!state.wild)state.wild={lastHit:0,scared:{},warning:''};
  let safeCamp=Math.hypot(hero.x+4,hero.z+2)<8&&state.built.shelter===3;
  let safeOutpost=Math.hypot(hero.x-12,hero.z-47)<7&&state.built.outpost===3;
  let highland=hero.z>35,swamp=hero.x<-34&&hero.z>0&&hero.z<30,fogForest=hero.x<-44&&hero.z>-14&&hero.z<18,razorRavine=hero.x>27&&hero.z>38,blackBeach=hero.x<-28&&hero.z<-38;
- let exposed=(state.weather==='storm'||state.weather==='rain'||(highland&&(dayValue()<.48)))&&!safeCamp&&!safeOutpost;let nearFire=Math.hypot(hero.x+4,hero.z+2)<5.2&&state.built.fire===3&&state.fireFuel>0;let gettingWet=(state.weather==='storm'||state.weather==='rain'||swamp)&&!safeCamp&&!safeOutpost;state.wet=clamp(state.wet+dt*(gettingWet?(state.weather==='storm'?.95:.52):(nearFire? -2.8:dayValue()>.55?-.75:-.28)),0,100);if(state.wet>55)state.exposure=clamp(state.exposure+dt*((state.wet-50)/100),0,100);if(state.weather==='heat'&&!safeCamp){state.water=clamp(state.water-dt*.085,0,100);state.energy=clamp(state.energy-dt*.03,0,100);}if(state.weather==='fog'&&!state.skills.nightEyes){state.energy=clamp(state.energy-dt*.035,0,100);}if(nearFire){state.exposure=clamp(state.exposure-dt*2.4,0,100);state.morale=clamp(state.morale+dt*.18,0,100);}else if(state.hp<35||state.water<15||state.food<15)state.morale=clamp(state.morale-dt*.12,0,100);if(state.morale<18)state.energy=clamp(state.energy-dt*.035,0,100);if(state.sickness>0){state.sickness=clamp(state.sickness-dt*.16,0,100);state.hp=clamp(state.hp-dt*.08,0,100);state.water=clamp(state.water-dt*.055,0,100);state.energy=clamp(state.energy-dt*.07,0,100);}
+ let exposed=(state.weather==='storm'||state.weather==='rain'||(highland&&(dayValue()<.48)))&&!safeCamp&&!safeOutpost;let nearFire=Math.hypot(hero.x+4,hero.z+2)<5.2&&state.built.fire===3&&state.fireFuel>0;let gettingWet=(state.weather==='storm'||state.weather==='rain'||swamp)&&!safeCamp&&!safeOutpost;state.wet=clamp(state.wet+dt*(gettingWet?(state.weather==='storm'?.95:.52):(nearFire? -2.8:dayValue()>.55?-.75:-.28)),0,100);if(state.wet>55)state.exposure=clamp(state.exposure+dt*((state.wet-50)/100),0,100);if(state.weather==='heat'&&!safeCamp){state.water=clamp(state.water-dt*.085,0,100);state.energy=clamp(state.energy-dt*.03,0,100);}if(state.weather==='fog'&&!state.skills.nightEyes){state.energy=clamp(state.energy-dt*.035,0,100);}if(nearFire){state.exposure=clamp(state.exposure-dt*2.4,0,100);state.morale=clamp(state.morale+dt*.18,0,100);}else if(state.hp<35||state.water<15||state.food<15)state.morale=clamp(state.morale-dt*.12,0,100);if(state.morale<18)state.energy=clamp(state.energy-dt*.035,0,100);if(state.sickness>0){state.sickness=clamp(state.sickness-dt*.16,0,100);state.hp=clamp(state.hp-dt*.08,0,100);state.water=clamp(state.water-dt*.055,0,100);state.energy=clamp(state.energy-dt*.07,0,100);}if(state.injuries.bleeding>0){state.injuries.bleeding=clamp(state.injuries.bleeding-dt*.10,0,100);state.hp=clamp(state.hp-dt*(state.injuries.bleeding/900),0,100);}if(state.injuries.infection>0){state.injuries.infection=clamp(state.injuries.infection-dt*.045,0,100);state.energy=clamp(state.energy-dt*(state.injuries.infection/1200),0,100);state.morale=clamp(state.morale-dt*.018,0,100);}if(state.injuries.fracture>0)state.injuries.fracture=clamp(state.injuries.fracture-dt*(state.built.shelter===3?.035:.012),0,100);
  let rate=exposed?(highland?1.15:.53)+(swamp?.32:0):-1.85;
  state.exposure=clamp((state.exposure||0)+rate*dt,0,100);
  if(state.exposure>79)state.hp=clamp(state.hp-dt*.38,0,100);
  if(swamp){state.water=clamp(state.water-dt*.065,0,100);state.energy=clamp(state.energy-dt*.10,0,100);}if(fogForest&&!state.skills.nightEyes){state.energy=clamp(state.energy-dt*.055,0,100);if(Math.floor(state.seconds)%31===0)flash('☁ Mlžný les tlumí orientaci. Sleduj minimapu.')}if(razorRavine&&running&&Math.floor(state.seconds)%17===0){state.hp=clamp(state.hp-4,0,100);flash('🪨 Ostré skály. Běh tady bolí.')}if(blackBeach&&state.clock>=19){state.exposure=clamp(state.exposure+dt*.45,0,100);}
- // Kanci mají vlastní teritorium, ale když tě uvidí, rozběhnou se po tobě.
+ // Drobná zvěř reaguje na hluk, plížení a vzdálenost; hadi mohou zaútočit sami.
+ for(const animal of OBJ){if(!['rabbit','crab','snake'].includes(animal.type)||!objectAvailable(animal))continue;if(!Number.isFinite(animal._wx)){animal._wx=animal.x;animal._wz=animal.z;animal._lastBite=-1e5;}let dx=hero.x-animal._wx,dz=hero.z-animal._wz,dist=Math.hypot(dx,dz),hearing=(state.noise/12)+(animal.type==='snake'?2.5:1.2);let detect=(crouching?2.3:3.5)+hearing;if(animal.type==='rabbit'&&dist<detect){let k=Math.max(.1,dist),speed=3.2,nx=animal._wx-dx/k*speed*dt,nz=animal._wz-dz/k*speed*dt;if(passable(nx,nz)){animal._wx=nx;animal._wz=nz;}}else if(animal.type==='crab'&&dist<detect*.75){let k=Math.max(.1,dist),nx=animal._wx-dx/k*1.6*dt,nz=animal._wz-dz/k*1.6*dt;if(passable(nx,nz)){animal._wx=nx;animal._wz=nz;}}else if(animal.type==='snake'&&dist<Math.max(1.7,detect*.55)){let k=Math.max(.1,dist),nx=animal._wx+dx/k*1.25*dt,nz=animal._wz+dz/k*1.25*dt;if(passable(nx,nz)){animal._wx=nx;animal._wz=nz;}if(dist<1.15&&state.seconds-animal._lastBite>7){animal._lastBite=state.seconds;state.hp=clamp(state.hp-7,0,100);state.sickness=clamp(state.sickness+24,0,100);state.injuries.infection=clamp(state.injuries.infection+12,0,100);flash('🐍 Had zaútočil ze zálohy. Jed a infekce se zhoršují.');}}else{animal._wx=mix(animal._wx,animal.x+Math.sin(elapsed*.25+animal.x)*1.7,clamp(dt*.18,0,1));animal._wz=mix(animal._wz,animal.z+Math.cos(elapsed*.22+animal.z)*1.4,clamp(dt*.18,0,1));}}
+ // Kanci mají vlastní teritorium, ale když tě uslyší nebo uvidí, rozběhnou se po tobě.
  for(const boar of OBJ){if(boar.type!=='boar')continue;
   if((state.wild.scared?.[boar.id]||0)>state.seconds||(state.wild.boarDown?.[boar.id]||0)>state.seconds)continue;
   if(!state.wild.boarHP)state.wild.boarHP={};if(!state.wild.boarDown)state.wild.boarDown={};
   if(!Number.isFinite(boar._wx)){boar._wx=boar.x+Math.sin(elapsed*.38+boar.x)*1.1;boar._wz=boar.z+Math.cos(elapsed*.38+boar.z)*.8;boar._aggro=0;}
-  let dx=hero.x-boar._wx,dz=hero.z-boar._wz,distance=Math.hypot(dx,dz),detect=10.5;
+  let dx=hero.x-boar._wx,dz=hero.z-boar._wz,distance=Math.hypot(dx,dz),detect=(crouching?7.0:9.0)+state.noise/20;
   if(distance<detect)boar._aggro=5.5;else boar._aggro=Math.max(0,(boar._aggro||0)-dt);
   if(boar._aggro>0&&distance>.05){let speed=distance<2.4?3.45:2.75,nx=boar._wx+dx/distance*speed*dt,nz=boar._wz+dz/distance*speed*dt;if(passable(nx,nz)){boar._wx=nx;boar._wz=nz;}distance=Math.hypot(hero.x-boar._wx,hero.z-boar._wz);}
   else{let hx=boar.x+Math.sin(elapsed*.38+boar.x)*1.1,hz=boar.z+Math.cos(elapsed*.38+boar.z)*.8;boar._wx=mix(boar._wx,hx,clamp(dt*.45,0,1));boar._wz=mix(boar._wz,hz,clamp(dt*.45,0,1));}
   if(distance<6.2&&state.seconds-(state.wild.warnAt||-1e5)>6){state.wild.warnAt=state.seconds;flash('🐗 Kanec tě zmerčil. Můžeš bojovat rukama, ale kopí je výrazně bezpečnější.');}
   if(distance<1.55&&state.seconds-(boar._lastCharge||-1e5)>2.15){boar._lastCharge=state.seconds;state.wild.lastHit=state.seconds;
-    let damage=activeWeapon()==='spear'||activeWeapon()==='hardenedSpear'?15:20;if(state.skills.toughness)damage*=.85;if(state.bag.leafArmor)damage*=state.skills.armor?.70:.84;damage=Math.max(5,Math.round(damage));state.hp=clamp(state.hp-damage,0,100);state.energy=clamp(state.energy-10,0,100);
+    let damage=activeWeapon()==='spear'||activeWeapon()==='hardenedSpear'?15:20;if(state.skills.toughness)damage*=.85;if(state.bag.leafArmor)damage*=state.skills.armor?.70:.84;damage=Math.max(5,Math.round(damage));state.hp=clamp(state.hp-damage,0,100);state.energy=clamp(state.energy-10,0,100);state.injuries.bleeding=clamp(state.injuries.bleeding+18,0,100);if(damage>=17)state.injuries.fracture=clamp(state.injuries.fracture+12,0,100);
     let pushX=(hero.x-boar._wx)/Math.max(distance,.1),pushZ=(hero.z-boar._wz)/Math.max(distance,.1),px=hero.x+pushX*.75,pz=hero.z+pushZ*.75;if(passable(px,pz)){hero.x=px;hero.z=pz;state.x=px;state.z=pz;}
     voiceEvent({type:'injury'});flash('🐗 Kanec tě nabral! -'+damage+' zdraví. '+(activeWeapon()!=='fists'?'Zaútoč přes AKCE.':'Braň se pěstmi nebo rychle vybav zbraň.'));changed=true;
   }