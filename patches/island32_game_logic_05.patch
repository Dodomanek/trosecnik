--- /mnt/data/trosecnik31_work/src/game_logic.js	2026-09-26 12:59:35.125216352 +0000
+++ /mnt/data/trosecnik32_work/src/game_logic.js	2026-09-26 13:35:00.355189330 +0000
@@ -753,7 +776,7 @@
 else if(type==='stash')finishAction(o,'gather',()=>visitStash(),1.25);
 else if(type==='crisis')finishAction(o,'gather',()=>openPanel('fate'),1.20);
 else if(type==='relay')repairRelay(o);
-else if(type==='site'){let site=o.site,stage=state.built[site]||0;if(site==='bridge'&&!state.flags.westTrail){flash('Nejdřív vyhledej stezku s kompasem.');return;}if(site==='pier'&&!state.flags.flares){flash('Nejdřív najdi signální světlici.');return;}if(site==='beacon'&&!state.flags.cave){flash('Nejdřív najdi rádiové jádro v jeskyni.');return;}if(stage<3){if(stage===0&&site==='fire'&&!state.flags['quest-fire']&&!has({cord:1,tinder:1})){flash('Ohniště vyžaduje pevný provaz a suchý troud. Vyrob je v nabídce Výroba.');return;}if(stage===0&&site==='shelter'&&!state.flags['quest-shelter']&&!has({cord:1})){flash('Na první etapu přístřešku potřebuješ pevný provaz.');return;}let full={...BUILD[site].needs};if(state.skills.builder&&full.wood)full.wood=Math.max(1,full.wood-1);let needs={};for(let [k,v] of Object.entries(full)){let already=Math.floor(v*stage/3);let next=Math.floor(v*(stage+1)/3);if(next>already)needs[k]=next-already;}if(!has(needs)){flash('Chybí materiál: '+costs(needs));return}finishAction(o,'build',()=>{if(!spend(needs))return;if(stage===0&&site==='fire'&&!state.flags['quest-fire'])spend({cord:1,tinder:1});if(stage===0&&site==='shelter'&&!state.flags['quest-shelter'])spend({cord:1});state.built[site]=stage+1;if(state.built[site]===3){flash('✓ Dokončeno: '+BUILD[site].name);if(!state.flags['build-reward-'+site]){state.flags['build-reward-'+site]=true;awardXP(site==='signal'?65:55,'Stavba: '+BUILD[site].name);}if(site==='bridge'){state.flags.bridgeBuilt=true;writeStory('Přes roklinu vede pevná lávka. Vozík na druhé straně se dá bezpečně prozkoumat.')}if(site==='pier'){state.flags.pierBuilt=true;writeStory('Na jižním pobřeží stojí molo. Teď už zbývá připravit evakuační zásoby.')}if(site==='fire')state.fireFuel=100;if(site==='signal'){state.flags.signal=true;state.journal.push('Zprovoznil jsem signální stanoviště. Na horizontu se objevila slabá odpověď světlem.')}}else flash(BUILD[site].name+' · konstrukce '+(stage+1)+'/3');checkQuest();save(true)},1.6)}else if(site==='shelter'||site==='outpost')finishAction(o,'rest',()=>{const comfort=site==='shelter'?campComfort():3;state.clock+=3;if(state.clock>=24){state.clock-=24;state.day++;}state.energy=100;state.hp=clamp(state.hp+10+comfort*3,0,100);state.exposure=0;state.wet=0;state.morale=clamp(state.morale+10+comfort*3,0,100);if(site==='shelter')state.flags.firstRest=true;checkQuest();flash('⛺ Odpočinek · komfort '+comfort+' · morálka '+Math.round(state.morale));save(true)},1.1);else if(site==='storage'){openPanel('storage')}else if(site==='rainCollector'){if(state.rainWater<10){flash('🌧 Zásobník je téměř prázdný. Počkej na déšť nebo bouři.');return;}finishAction(o,'drink',()=>{const amount=Math.min(45,state.rainWater);state.rainWater-=amount;if(state.bag.clayFlask>0&&!state.flags.flaskFilled&&state.rainWater>=18){state.rainWater-=18;state.flags.flaskFilled=true;flash('🏺 Napil ses a naplnil čutoru dešťovou vodou.');}state.water=clamp(state.water+amount,0,100);state.morale=clamp(state.morale+3,0,100);save(true);updateUI()},.9);}else if(site==='filter')finishAction(o,'drink',()=>{if(state.flags.lastFilterDay===state.day){flash('Filtr bude mít další vodu až zítra.');return}state.flags.lastFilterDay=state.day;state.flags.filteredFirst=true;checkQuest();state.water=clamp(state.water+70,0,100);if(state.bag.clayFlask>0&&!state.flags.flaskFilled){state.flags.flaskFilled=true;flash('🏺 Čutora naplněna filtrovanou vodou.');}flash('💧 Filtrovaná voda doplnila zásoby.');save(true)},1.1);else if(site==='fire'){if(state.fireFuel<=3){if(state.bag.wood<=0){flash('🔥 Oheň vyhasl. Přines dřevo a znovu ho přilož.');return;}finishAction(o,'build',()=>{state.bag.wood--;state.fireFuel=clamp(state.fireFuel+48,0,100);state.wet=clamp(state.wet-18,0,100);state.morale=clamp(state.morale+4,0,100);flash('🔥 Oheň znovu hoří · palivo '+Math.round(state.fireFuel)+' %');save(true)},.85)}else if(state.bag.fish){finishAction(o,'build',()=>{state.bag.fish--;state.fireFuel=clamp(state.fireFuel-8,0,100);state.food=clamp(state.food+52,0,100);state.morale=clamp(state.morale+5,0,100);state.flags.cookedFirstFish=true;checkQuest();flash('🔥 Ryba je upečená a snědená.');save(true)},1.1)}else if(state.bag.wood){finishAction(o,'build',()=>{state.bag.wood--;state.fireFuel=clamp(state.fireFuel+38,0,100);state.wet=clamp(state.wet-12,0,100);flash('🪵 Přiloženo do ohně · palivo '+Math.round(state.fireFuel)+' %');save(true)},.7)}else flash('🔥 Oheň hoří na '+Math.round(state.fireFuel)+' %. Přines rybu nebo dřevo.')}else if(site==='pier'){if(currentQuest()?.id!=='evac'){flash('Molo je připravené. Zbývá výbava na cestu a správný čas příjezdu člunu.');return;}if(state.clock>=5&&state.clock<=9&&state.weather!=='storm'){flash('Záchranný člun může připlout právě teď. Aktivuj evakuační bod vedle mola.');return;}if(!state.bag.ration){flash('Na vyčkání u pobřeží si připrav expediční zásoby.');return;}finishAction(o,'rest',()=>{state.bag.ration--;state.day+=state.clock>=9?1:0;state.clock=6;state.energy=clamp(state.energy+18,0,100);state.food=clamp(state.food+12,0,100);state.water=clamp(state.water+12,0,100);flash('🌅 Přečkal jsi noc na molu. Je 06:00, vyšli světlici na evakuačním bodě.');writeStory('Na molu jsem spotřeboval expediční balíček a vyčkal na ranní příliv.');save(true);updateUI()},2.2);}else if(site==='beacon'){if(state.flags.beacon){flash('📡 Maják již odvysílal souřadnice.');return;}if(!state.bag.core||!state.bag.lens){flash('Chybí rádiové jádro nebo optická čočka z expedice.');return;}if(dayValue()<.42||state.weather==='storm'){flash('Maják lze aktivovat pouze za dne a bez bouře.');return;}finishAction(o,'build',()=>{state.bag.core--;state.bag.lens--;state.flags.beacon=true;writeStory('Nouzový maják odvysílal souřadnice. '+(state.story.fate==='mara'?'Ivo dál sleduje frekvenci.':'Mára hlídá pobřeží.')+' Ostrov už není bezejmenný.');awardXP(90,'Nouzové vysílání');checkQuest();save(true)},3.6);}else flash('📡 Signální stanoviště vysílá. Sleduj horizont.')}
+else if(type==='site'){let site=o.site,stage=state.built[site]||0;if(stage===3&&siteDurability(site)<40){const rc={wood:1,fiber:1};if(!has(rc)){flash('🔧 '+BUILD[site].name+' je poškozená ('+Math.round(siteDurability(site))+' %). Oprava: '+costs(rc));return;}finishAction(o,'build',()=>{if(!spend(rc))return;state.durability[site]=clamp(siteDurability(site)+62,0,100);flash('🔧 Opraveno: '+BUILD[site].name+' · '+Math.round(state.durability[site])+' %');writeStory('Opravil jsem '+BUILD[site].name.toLowerCase()+' po poškození bouří.');save(true)},1.4);return;}if(site==='bridge'&&!state.flags.westTrail){flash('Nejdřív vyhledej stezku s kompasem.');return;}if(site==='pier'&&!state.flags.flares){flash('Nejdřív najdi signální světlici.');return;}if(site==='beacon'&&!state.flags.cave){flash('Nejdřív najdi rádiové jádro v jeskyni.');return;}if(stage<3){if(stage===0&&site==='fire'&&!state.flags['quest-fire']&&!has({cord:1,tinder:1})){flash('Ohniště vyžaduje pevný provaz a suchý troud. Vyrob je v nabídce Výroba.');return;}if(stage===0&&site==='shelter'&&!state.flags['quest-shelter']&&!has({cord:1})){flash('Na první etapu přístřešku potřebuješ pevný provaz.');return;}let full={...BUILD[site].needs};if(state.skills.builder&&full.wood)full.wood=Math.max(1,full.wood-1);let needs={};for(let [k,v] of Object.entries(full)){let already=Math.floor(v*stage/3);let next=Math.floor(v*(stage+1)/3);if(next>already)needs[k]=next-already;}if(!has(needs)){flash('Chybí materiál: '+costs(needs));return}finishAction(o,'build',()=>{if(!spend(needs))return;if(stage===0&&site==='fire'&&!state.flags['quest-fire'])spend({cord:1,tinder:1});if(stage===0&&site==='shelter'&&!state.flags['quest-shelter'])spend({cord:1});state.built[site]=stage+1;if(state.built[site]===3){state.durability[site]=100;flash('✓ Dokončeno: '+BUILD[site].name);if(!state.flags['build-reward-'+site]){state.flags['build-reward-'+site]=true;awardXP(site==='signal'?65:55,'Stavba: '+BUILD[site].name);}if(site==='bridge'){state.flags.bridgeBuilt=true;writeStory('Přes roklinu vede pevná lávka. Vozík na druhé straně se dá bezpečně prozkoumat.')}if(site==='pier'){state.flags.pierBuilt=true;writeStory('Na jižním pobřeží stojí molo. Teď už zbývá připravit evakuační zásoby.')}if(site==='fire')state.fireFuel=100;if(site==='signal'){state.flags.signal=true;state.journal.push('Zprovoznil jsem signální stanoviště. Na horizontu se objevila slabá odpověď světlem.')}}else flash(BUILD[site].name+' · konstrukce '+(stage+1)+'/3');checkQuest();save(true)},1.6)}else if(site==='trapLine'){if(state.trapLastDay===state.day){flash('🪤 Pasti jsou dnes prázdné. Zkontroluj je další den.');return;}finishAction(o,'gather',()=>{state.trapLastDay=state.day;const rich=(state.day%3===0);add('meat',rich?2:1);if(rich)add('hide',1);state.morale=clamp(state.morale+3,0,100);awardXP(rich?24:15,'Kontrola loveckých pastí');flash('🪤 Pasti: '+(rich?'2× maso + kůže':'1× maso'));save(true)},1.0);}else if(site==='dryRack'){if((state.bag.meat||0)<=0){flash('🍖 Na sušáku není co zpracovat. Přines syrové maso.');return;}finishAction(o,'build',()=>{state.bag.meat--;add('smokedMeat',1);if(state.bag.meat<=0)state.meatSpoilAt=0;state.morale=clamp(state.morale+2,0,100);flash('🍖 Maso je vyuzené a vydrží celou výpravu.');save(true)},1.4);}else if(site==='shelter'||site==='outpost')finishAction(o,'rest',()=>{const comfort=site==='shelter'?campComfort():3;state.clock+=3;if(state.clock>=24){state.clock-=24;state.day++;}state.energy=100;state.hp=clamp(state.hp+10+comfort*3,0,100);state.exposure=0;state.wet=0;state.morale=clamp(state.morale+10+comfort*3,0,100);if(site==='shelter')state.flags.firstRest=true;checkQuest();flash('⛺ Odpočinek · komfort '+comfort+' · morálka '+Math.round(state.morale));save(true)},1.1);else if(site==='storage'){openPanel('storage')}else if(site==='rainCollector'){if(state.rainWater<10){flash('🌧 Zásobník je téměř prázdný. Počkej na déšť nebo bouři.');return;}finishAction(o,'drink',()=>{const amount=Math.min(45,state.rainWater);state.rainWater-=amount;if(state.bag.clayFlask>0&&!state.flags.flaskFilled&&state.rainWater>=18){state.rainWater-=18;state.flags.flaskFilled=true;flash('🏺 Napil ses a naplnil čutoru dešťovou vodou.');}state.water=clamp(state.water+amount,0,100);state.morale=clamp(state.morale+3,0,100);save(true);updateUI()},.9);}else if(site==='filter')finishAction(o,'drink',()=>{if(state.flags.lastFilterDay===state.day){flash('Filtr bude mít další vodu až zítra.');return}state.flags.lastFilterDay=state.day;state.flags.filteredFirst=true;checkQuest();state.water=clamp(state.water+70,0,100);if(state.bag.clayFlask>0&&!state.flags.flaskFilled){state.flags.flaskFilled=true;flash('🏺 Čutora naplněna filtrovanou vodou.');}flash('💧 Filtrovaná voda doplnila zásoby.');save(true)},1.1);else if(site==='fire'){if(state.fireFuel<=3){if(state.bag.wood<=0){flash('🔥 Oheň vyhasl. Přines dřevo a znovu ho přilož.');return;}finishAction(o,'build',()=>{state.bag.wood--;state.fireFuel=clamp(state.fireFuel+48,0,100);state.wet=clamp(state.wet-18,0,100);state.morale=clamp(state.morale+4,0,100);flash('🔥 Oheň znovu hoří · palivo '+Math.round(state.fireFuel)+' %');save(true)},.85)}else if(state.bag.fish){finishAction(o,'build',()=>{state.bag.fish--;state.fireFuel=clamp(state.fireFuel-8,0,100);state.food=clamp(state.food+52,0,100);state.morale=clamp(state.morale+5,0,100);state.flags.cookedFirstFish=true;checkQuest();flash('🔥 Ryba je upečená a snědená.');save(true)},1.1)}else if(state.bag.wood){finishAction(o,'build',()=>{state.bag.wood--;state.fireFuel=clamp(state.fireFuel+38,0,100);state.wet=clamp(state.wet-12,0,100);flash('🪵 Přiloženo do ohně · palivo '+Math.round(state.fireFuel)+' %');save(true)},.7)}else flash('🔥 Oheň hoří na '+Math.round(state.fireFuel)+' %. Přines rybu nebo dřevo.')}else if(site==='pier'){if(currentQuest()?.id!=='evac'){flash('Molo je připravené. Zbývá výbava na cestu a správný čas příjezdu člunu.');return;}if(state.clock>=5&&state.clock<=9&&state.weather!=='storm'){flash('Záchranný člun může připlout právě teď. Aktivuj evakuační bod vedle mola.');return;}if(!state.bag.ration){flash('Na vyčkání u pobřeží si připrav expediční zásoby.');return;}finishAction(o,'rest',()=>{state.bag.ration--;state.day+=state.clock>=9?1:0;state.clock=6;state.energy=clamp(state.energy+18,0,100);state.food=clamp(state.food+12,0,100);state.water=clamp(state.water+12,0,100);flash('🌅 Přečkal jsi noc na molu. Je 06:00, vyšli světlici na evakuačním bodě.');writeStory('Na molu jsem spotřeboval expediční balíček a vyčkal na ranní příliv.');save(true);updateUI()},2.2);}else if(site==='beacon'){if(state.flags.beacon){flash('📡 Maják již odvysílal souřadnice.');return;}if(!state.bag.core||!state.bag.lens){flash('Chybí rádiové jádro nebo optická čočka z expedice.');return;}if(dayValue()<.42||state.weather==='storm'){flash('Maják lze aktivovat pouze za dne a bez bouře.');return;}finishAction(o,'build',()=>{state.bag.core--;state.bag.lens--;state.flags.beacon=true;writeStory('Nouzový maják odvysílal souřadnice. '+(state.story.fate==='mara'?'Ivo dál sleduje frekvenci.':'Mára hlídá pobřeží.')+' Ostrov už není bezejmenný.');awardXP(90,'Nouzové vysílání');checkQuest();save(true)},3.6);}else flash('📡 Signální stanoviště vysílá. Sleduj horizont.')}
 }
 
 function writeStory(entry){state.journal.push('Den '+state.day+': '+entry);state.journal=state.journal.slice(-100);changed=true}