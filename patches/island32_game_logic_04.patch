--- /mnt/data/trosecnik31_work/src/game_logic.js	2026-09-26 12:59:35.125216352 +0000
+++ /mnt/data/trosecnik32_work/src/game_logic.js	2026-09-26 13:35:00.355189330 +0000
@@ -557,7 +576,7 @@
  let safeCamp=Math.hypot(hero.x+4,hero.z+2)<8&&state.built.shelter===3;
  let safeOutpost=Math.hypot(hero.x-12,hero.z-47)<7&&state.built.outpost===3;
  let highland=hero.z>35,swamp=hero.x<-34&&hero.z>0&&hero.z<30,fogForest=hero.x<-44&&hero.z>-14&&hero.z<18,razorRavine=hero.x>27&&hero.z>38,blackBeach=hero.x<-28&&hero.z<-38;
- let exposed=(state.weather==='storm'||state.weather==='rain'||(highland&&(dayValue()<.48)))&&!safeCamp&&!safeOutpost;let nearFire=Math.hypot(hero.x+4,hero.z+2)<5.2&&state.built.fire===3&&state.fireFuel>0;let gettingWet=(state.weather==='storm'||state.weather==='rain'||swamp)&&!safeCamp&&!safeOutpost;state.wet=clamp(state.wet+dt*(gettingWet?(state.weather==='storm'?.95:.52):(nearFire? -2.8:dayValue()>.55?-.75:-.28)),0,100);if(state.wet>55)state.exposure=clamp(state.exposure+dt*((state.wet-50)/100),0,100);if(state.weather==='heat'&&!safeCamp){state.water=clamp(state.water-dt*.085,0,100);state.energy=clamp(state.energy-dt*.03,0,100);}if(state.weather==='fog'&&!state.skills.nightEyes){state.energy=clamp(state.energy-dt*.035,0,100);}if(nearFire){state.exposure=clamp(state.exposure-dt*2.4,0,100);state.morale=clamp(state.morale+dt*.18,0,100);}else if(state.hp<35||state.water<15||state.food<15)state.morale=clamp(state.morale-dt*.12,0,100);if(state.morale<18)state.energy=clamp(state.energy-dt*.035,0,100);
+ let exposed=(state.weather==='storm'||state.weather==='rain'||(highland&&(dayValue()<.48)))&&!safeCamp&&!safeOutpost;let nearFire=Math.hypot(hero.x+4,hero.z+2)<5.2&&state.built.fire===3&&state.fireFuel>0;let gettingWet=(state.weather==='storm'||state.weather==='rain'||swamp)&&!safeCamp&&!safeOutpost;state.wet=clamp(state.wet+dt*(gettingWet?(state.weather==='storm'?.95:.52):(nearFire? -2.8:dayValue()>.55?-.75:-.28)),0,100);if(state.wet>55)state.exposure=clamp(state.exposure+dt*((state.wet-50)/100),0,100);if(state.weather==='heat'&&!safeCamp){state.water=clamp(state.water-dt*.085,0,100);state.energy=clamp(state.energy-dt*.03,0,100);}if(state.weather==='fog'&&!state.skills.nightEyes){state.energy=clamp(state.energy-dt*.035,0,100);}if(nearFire){state.exposure=clamp(state.exposure-dt*2.4,0,100);state.morale=clamp(state.morale+dt*.18,0,100);}else if(state.hp<35||state.water<15||state.food<15)state.morale=clamp(state.morale-dt*.12,0,100);if(state.morale<18)state.energy=clamp(state.energy-dt*.035,0,100);if(state.sickness>0){state.sickness=clamp(state.sickness-dt*.16,0,100);state.hp=clamp(state.hp-dt*.08,0,100);state.water=clamp(state.water-dt*.055,0,100);state.energy=clamp(state.energy-dt*.07,0,100);}
  let rate=exposed?(highland?1.15:.53)+(swamp?.32:0):-1.85;
  state.exposure=clamp((state.exposure||0)+rate*dt,0,100);
  if(state.exposure>79)state.hp=clamp(state.hp-dt*.38,0,100);
@@ -581,7 +600,7 @@
  if(state.hp<=0)handleDeath('útok nebo vyčerpání');
 }
 function finishAction(o,kind,work,duration=1.05){if(pending)return;pending={o,kind,work,duration,remaining:duration};hero.goal=null;autogoal=null;hero.working=.01;hero.kind=kind;hero.yaw=facing(o.x-hero.x,o.z-hero.z);}
-function add(id,n=1){state.bag[id]=(state.bag[id]||0)+n;changed=true;if(['wood','stone','fiber','coconut','scrap','herb','reeds','clay','flint','berries','salt'].includes(id))voiceEvent({type:'gather'});flash('+'+n+' · '+ITEM[id]?.[1]);}
+function add(id,n=1){state.bag[id]=(state.bag[id]||0)+n;if(id==='meat')state.meatSpoilAt=state.seconds+240;changed=true;if(['wood','stone','fiber','coconut','scrap','herb','reeds','clay','flint','berries','salt'].includes(id))voiceEvent({type:'gather'});flash('+'+n+' · '+ITEM[id]?.[1]);}
 function has(items){return Object.entries(items).every(([id,n])=>(state.bag[id]||0)>=n)}
 function spend(items){if(!has(items))return false;for(let [id,n] of Object.entries(items))state.bag[id]-=n;changed=true;return true}
 function costs(items){return Object.entries(items).map(([id,n])=>(ITEM[id]?.[0]||'◈')+' '+(ITEM[id]?.[1]||id)+' ×'+n).join(' · ')}
@@ -698,7 +717,7 @@
  if(id==='flareCache'){finishAction(o,'gather',()=>{f.flares=true;add('flare');add('wood',3);add('fiber',2);writeStory('V jihozápadním skladu zůstala poslední použitelná světlice.');checkQuest();save(true)},2.4);return;}
  if(id==='evac'){if(!f.pierBuilt||!f.finalPack||!state.bag.flare||!state.bag.chart){flash('Připrav molo, finální zásoby, světlici a mapu.');return;}if(state.weather==='storm'||state.clock<5||state.clock>9){flash('Záchranný člun připlouvá mezi 05:00 a 09:00 pouze bez bouře. Odpočiň si a vrať se ráno.');return;}if(state.hp<50||state.water<40||state.energy<40){flash('Na poslední cestu potřebuješ 50 zdraví, 40 vody a 40 energie.');return;}finishAction(o,'build',()=>{f.evacuated=true;state.bag.flare--;writeStory(state.story.fate==='mara'?'Jana s Ivem mě doprovodili na molo. Signál dorazil ke člunu, ale z ostrova se vracíme s těžkým tajemstvím.':'Jana s Márou mě doprovodili na molo. Světlice přivolala člun; Mára teď musí čelit minulosti.');checkQuest();save(true);openPanel('epilogue');},4.5);return;}
 }
-function interactObject(o){if(!o||pending||mini||pane)return;if(o.type==='crate'&&!state.flags.crate)voiceEvent({type:'crate-approach'});let d=d2(hero,o);let reach=o.type==='boar'?weaponStats().range:1.10;if(d>o.r+reach){let ox=o.type==='boar'?(o._wx??o.x):o.x,oz=o.type==='boar'?(o._wz??o.z):o.z;autogoal={x:ox,z:oz,r:o.r,o};hero.target=o;flash(o.type==='boar'?'Přibližuješ se ke kanci. Připrav se na útok.':'Robinson se přesouvá k cíli.');return}beginInteraction(o)}
+function interactObject(o){if(!o||pending||mini||pane)return;if(o.type==='crate'&&!state.flags.crate)voiceEvent({type:'crate-approach'});let d=d2(hero,o);let reach=['boar','rabbit','snake'].includes(o.type)?weaponStats().range:1.10;if(d>o.r+reach){let ox=o.type==='boar'?(o._wx??o.x):o.x,oz=o.type==='boar'?(o._wz??o.z):o.z;autogoal={x:ox,z:oz,r:o.r,o};hero.target=o;flash(o.type==='boar'?'Přibližuješ se ke kanci. Připrav se na útok.':'Robinson se přesouvá k cíli.');return}beginInteraction(o)}
 function beginInteraction(o){if(pending||mini||pane||!objectAvailable(o))return;hero.target=o;let type=o.type;
 if(type==='storySite'){interactStorySite(o);return;}
 if(type==='resin'){finishAction(o,'gather',()=>{add('resin');state.flags.resinCollected=(state.flags.resinCollected||0)+1;recordGather(o,state.flags,state.seconds);checkQuest();save(true)},1.8);return;}
@@ -735,6 +754,10 @@
   else flash((w.id==='fists'?'👊':'⚔')+' '+w.name+' · zásah '+Math.round(w.damage)+' · Kanec '+Math.max(0,Math.ceil(hp))+' / 96');save(true);
  },w.duration);
 }
+else if(type==='rabbit'){if((state.wild.animalDown?.[o.id]||0)>state.seconds)return;let w=weaponStats();if(w.id==='fists'){flash('🐇 Králík je příliš rychlý. Použij nůž, kopí, sekeru nebo kyj.');return;}if(state.energy<w.cost){flash('Nemáš dost energie na lov.');return;}finishAction(o,w.id.includes('spear')?'thrust':'chop',()=>{state.energy=clamp(state.energy-w.cost,0,100);state.weaponEdge=clamp(state.weaponEdge-2,0,100);state.wild.animalDown[o.id]=state.seconds+360;add('meat',1);if((state.day+o.id.length)%2===0)add('hide',1);awardXP(28,'Lov králíka');writeStory('Ulovený králík dal čerstvé maso'+(((state.day+o.id.length)%2===0)?' a použitelnou kůži':'')+'.');save(true)},w.duration);}
+else if(type==='crab'){if((state.wild.animalDown?.[o.id]||0)>state.seconds)return;finishAction(o,'gather',()=>{state.wild.animalDown[o.id]=state.seconds+240;add('meat',1);awardXP(14,'Sběr kraba');save(true)},.8);}
+else if(type==='snake'){if((state.wild.animalDown?.[o.id]||0)>state.seconds)return;let w=weaponStats();if(state.energy<w.cost){flash('Nemáš sílu na střet s hadem.');return;}finishAction(o,w.id==='fists'?'fight':(w.id.includes('spear')?'thrust':'chop'),()=>{state.energy=clamp(state.energy-w.cost,0,100);if(w.id==='fists'){state.hp=clamp(state.hp-12,0,100);state.sickness=clamp(state.sickness+55,0,100);flash('🐍 Had tě kousl. Jed způsobuje nevolnost.');}else{state.weaponEdge=clamp(state.weaponEdge-3,0,100);add('meat',1);flash('🐍 Had je mrtvý. Maso rychle zpracuj.');}state.wild.animalDown[o.id]=state.seconds+480;awardXP(w.id==='fists'?45:30,'Jedovatý had');save(true)},w.duration);}
+else if(type==='islandStory'){if(o.id==='hunterCamp'){if(state.flags.hunterNote){flash('🏕 Starý tábor lovce je už prohledaný. Zápis ukazuje k vodopádu.');return;}finishAction(o,'gather',()=>{state.flags.hunterNote=true;add('hide',1);add('bandage',1);awardXP(55,'Opuštěný tábor lovce');writeStory('V opuštěném táboře lovce jsem našel zápis: „Za vodopádem je značka z černého kamene.“');save(true)},1.8);return;}if(o.id==='waterfallBasin'){if(!state.flags.hunterNote){flash('💧 Vodopád je působivý, ale bez stopy nevíš, co hledat.');return;}if(state.flags.hunterTrail){flash('💧 Značka u vodopádu už je zaznamenaná. Ukazuje na východní pobřeží.');return;}finishAction(o,'gather',()=>{state.flags.hunterTrail=true;add('herb',1);awardXP(65,'Stopa u vodopádu');writeStory('Za vodopádem je vyrytý symbol pěti čedičových sloupů. Lovcova skrýš musí být na východním pobřeží.');save(true)},2.0);return;}if(o.id==='basaltCove'){if(!state.flags.hunterTrail){flash('🪨 Mezi sloupy nic nenacházíš. Chybí ti stopa od vodopádu.');return;}if(state.flags.hunterCache){flash('🪨 Lovcova skrýš je prázdná.');return;}finishAction(o,'gather',()=>{state.flags.hunterCache=true;add('smokedMeat',2);add('hide',2);add('scrap',1);awardXP(110,'Lovcova skrytá zásoba');state.morale=clamp(state.morale+12,0,100);writeStory('V čedičové zátoce jsem našel poslední zásobu starého lovce: trvanlivé maso, kůže a kovový díl.');save(true)},2.3);return;}}
 else if(type==='cave'){
  if(state.flags.cave){flash('V jeskyni jsi už vyzvedl rádiové jádro.');return;}
  if(!state.bag.spear){flash('Stezka k jeskyni je nebezpečná. Nejprve vyrob kopí.');return;}