--- /mnt/data/trosecnik31_work/src/game_logic.js	2026-09-26 12:59:35.125216352 +0000
+++ /mnt/data/trosecnik32_work/src/game_logic.js	2026-09-26 13:35:00.355189330 +0000
@@ -354,9 +366,13 @@
 if(o.type==='lagoon'){shape('disc',x,y+.055,z,3.4,1,3.0,[.22,.61,.62]);for(let j=0;j<6;j++){let a=j*1.047;shape('sphere',x+Math.cos(a)*1.72,y+.12,z+Math.sin(a)*1.48,.40,.35,.36,j%2?C.stoneLight:C.palm);}shape('cyl',x,y+.40,z,.10,.8,.10,C.metal);shape('sphere',x,y+.87,z,.39,.32,.39,C.red);}
 if(o.type==='resin'){shape('cyl',x,y+.38,z,.34,.76,.31,C.bark);shape('sphere',x+.23,y+.61,z-.20,.19,.27,.16,C.gold);}
 if(o.type==='storySite'){const tall=['eastStation','eastGenerator','terminal','evac','southMarker'].includes(o.id);if(o.phase==='fieldCache'){shape('box',x,y+.31,z,1.15,.55,.75,C.wood,.23);shape('box',x,y+.64,z,1.17,.11,.78,C.paleWood,.23);for(let side of [-1,1])shape('box',x+side*.39,y+.32,z,.09,.64,.82,C.darkMetal,.23);shape('sphere',x,y+.74,z,.11,.09,.11,C.gold);}else{shape('box',x,y+.38,z,tall?1.55:1.05,.69,tall?1.15:.77,C.darkMetal,.18);shape('box',x,y+.77,z,tall?1.58:1.09,.12,tall?1.19:.80,C.stoneLight,.18);shape('sphere',x,y+1.04,z,.17,.16,.17,C.gold);if(tall){shape('cyl',x,y+1.60,z,.08,1.15,.08,C.metal);shape('sphere',x,y+2.19,z,.18,.18,.18,C.gold)}}}
+if(o.type==='rabbit'){shape('sphere',x,y+.31,z,.50,.34,.36,C.stoneLight);shape('sphere',x-.31,y+.40,z,.31,.27,.29,C.white);for(let side of [-1,1])shape('cone',x-.38,y+.73,z+side*.12,.08,.38,.08,C.stoneLight,0,.08);shape('sphere',x-.45,y+.44,z-.19,.04,.05,.04,C.eye);}
+if(o.type==='crab'){shape('sphere',x,y+.16,z,.55,.22,.43,C.red);for(let side of [-1,1]){for(let j=0;j<3;j++)shape('cyl',x+side*(.32+j*.11),y+.10,z+(j-1)*.18,.035,.38,.035,C.red,side*.9);shape('sphere',x+side*.49,y+.22,z-.10,.20,.13,.18,C.red);}shape('sphere',x-.14,y+.28,z-.30,.045,.05,.045,C.white);shape('sphere',x+.14,y+.28,z-.30,.045,.05,.045,C.white);}
+if(o.type==='snake'){for(let j=0;j<7;j++){let a=j*.72+t*.15;shape('sphere',x+Math.cos(a)*(.18+j*.055),y+.09,z+Math.sin(a)*(.16+j*.04),.22,.13,.22,j===0?C.leaf2:C.deep)}shape('sphere',x-.42,y+.14,z-.18,.29,.18,.24,C.leaf2);shape('sphere',x-.53,y+.17,z-.31,.035,.04,.035,C.red);}
+if(o.type==='islandStory'){if(o.id==='hunterCamp'){shape('box',x,y+.30,z,1.65,.42,1.1,C.wood,.25);shape('box',x,y+.93,z,2.05,.12,1.45,C.palm,.25);shape('cyl',x-.72,y+.67,z-.38,.10,1.35,.10,C.wood);shape('cyl',x+.72,y+.67,z+.38,.10,1.35,.10,C.wood);}else if(o.id==='waterfallBasin'){shape('disc',x,y+.04,z,2.5,1,2.0,C.water);for(let j=0;j<5;j++)shape('sphere',x+(j-2)*.52,y+.25+j*.14,z+.55,.48,.55,.42,j%2?C.stone:C.stoneLight);shape('box',x,y+1.55,z+.70,.32,2.5,.20,C.water);}else{for(let j=-2;j<=2;j++)shape('cyl',x+j*.42,y+.65+(j%2)*.25,z,.28,1.3+(j%2)*.5,.28,C.darkMetal);shape('box',x,y+.28,z-.70,1.1,.48,.75,C.wood,.18);}}
 if(o.type==='boar'){if(state.wild?.scared?.[o.id]>state.seconds||state.wild?.boarDown?.[o.id]>state.seconds)return;let wx=o._wx??(x+Math.sin(elapsed*.38+o.x)*1.1),wz=o._wz??(z+Math.cos(elapsed*.38+o.z)*.8),hy=H(wx,wz);shape('sphere',wx,hy+.61,wz,.80,.59,.45,C.bark);shape('sphere',wx-.45,hy+.70,wz-.21,.43,.34,.36,C.wood);for(let j of [-1,1])for(let k of [-1,1])shape('cyl',wx+j*.42,hy+.27,wz+k*.30,.11,.45,.11,C.bark);for(let k of [-1,1])shape('cone',wx-.76,hy+.65,wz+k*.19,.13,.25,.11,C.stoneLight,.30);shape('disc',wx,hy+.02,wz,1.05,1,1.05,C.red);}
 if(o.type==='site'){let stage=state.built[o.site];if(stage<3){for(let j=0;j<4;j++){let a=j*1.57;shape('cyl',x+Math.cos(a)*.7,y+.30,z+Math.sin(a)*.7,.10,.60,.10,C.paleWood)}for(let j=0;j<stage;j++){shape('box',x+(j-.5)*.27,y+.30+j*.13,z,.65,.15,.14,C.wood)}if(stage===0)shape('disc',x,y+.03,z,1.5,1,1.4,C.rope)}
-if(stage>=3){if(o.site==='storage'){shape('box',x,y+.45,z,1.45,.72,.95,C.wood,.17);shape('box',x,y+.85,z,1.53,.13,1.01,C.paleWood,.17);for(const side of [-1,1])shape('box',x+side*.49,y+.48,z,.10,.72,1.00,C.darkMetal,.17);shape('box',x,y+.52,z+.51,.19,.22,.08,C.gold,.17)}if(o.site==='rainCollector'){for(let side of [-1,1])shape('cyl',x+side*.63,y+.86,z,.08,1.72,.08,C.wood);shape('box',x,y+1.66,z,1.55,.10,1.25,C.palm);shape('cone',x,y+1.10,z,1.22,.95,1.10,C.palm2);shape('cyl',x,y+.30,z,.72,.55,.72,C.darkMetal);shape('disc',x,y+.59,z,.58,1,.58,C.water)}if(o.site==='outpost'){shape('box',x,y+1.5,z,3.2,.19,2.6,C.palm);for(let k of [-1,1])for(let j of [-1,1])shape('cyl',x+k*1.4,y+.85,z+j*1.05,.14,1.7,.14,C.wood);shape('box',x,y+.28,z,2.35,.35,1.50,C.wood);}if(o.site==='beacon'){shape('cyl',x,y+3.3,z,.22,6.4,.22,C.metal);shape('box',x,y+5.6,z,1.9,.16,.22,C.darkMetal);shape('cone',x,y+6.62,z,.55,1.3,.55,C.red);}if(o.site==='fire'){if(!tryDrawGLB('campfire',T(x,y,z,1,1,1,0))){for(let j=0;j<8;j++){let a=j*Math.PI/4;shape('sphere',x+Math.cos(a)*.56,y+.12,z+Math.sin(a)*.56,.33,.22,.33,C.stone)}for(let j=0;j<3;j++){let a=j*2.1;shape('cyl',x+Math.sin(a)*.11,y+.18,z+Math.cos(a)*.09,.15,.95,.15,C.wood,a+Math.PI/2)}}if(state.fireFuel>0)for(let j=0;j<3;j++)shape('cone',x+Math.sin(j*2)*.09,y+.55+Math.sin(t*9+j)*.10,z+Math.cos(j*2)*.09,.33,.90,.33,j%2?C.fire:C.gold)}
+if(stage>=3){if(o.site==='trapLine'){for(let side of [-1,1]){shape('cyl',x+side*.65,y+.52,z,.07,1.05,.07,C.wood);shape('box',x+side*.65,y+.97,z,.42,.07,.08,C.rope)}shape('box',x,y+.13,z,1.35,.10,.85,C.rope,.16)}if(o.site==='dryRack'){for(let side of [-1,1])shape('cyl',x+side*.72,y+.72,z,.09,1.45,.09,C.wood);shape('box',x,y+1.37,z,1.65,.09,.10,C.wood);for(let j=-1;j<=1;j++){shape('box',x+j*.42,y+.92,z,.24,.42,.09,C.red);shape('cyl',x+j*.42,y+1.19,z,.025,.30,.025,C.rope)}}if(o.site==='storage'){shape('box',x,y+.45,z,1.45,.72,.95,C.wood,.17);shape('box',x,y+.85,z,1.53,.13,1.01,C.paleWood,.17);for(const side of [-1,1])shape('box',x+side*.49,y+.48,z,.10,.72,1.00,C.darkMetal,.17);shape('box',x,y+.52,z+.51,.19,.22,.08,C.gold,.17)}if(o.site==='rainCollector'){for(let side of [-1,1])shape('cyl',x+side*.63,y+.86,z,.08,1.72,.08,C.wood);shape('box',x,y+1.66,z,1.55,.10,1.25,C.palm);shape('cone',x,y+1.10,z,1.22,.95,1.10,C.palm2);shape('cyl',x,y+.30,z,.72,.55,.72,C.darkMetal);shape('disc',x,y+.59,z,.58,1,.58,C.water)}if(o.site==='outpost'){shape('box',x,y+1.5,z,3.2,.19,2.6,C.palm);for(let k of [-1,1])for(let j of [-1,1])shape('cyl',x+k*1.4,y+.85,z+j*1.05,.14,1.7,.14,C.wood);shape('box',x,y+.28,z,2.35,.35,1.50,C.wood);}if(o.site==='beacon'){shape('cyl',x,y+3.3,z,.22,6.4,.22,C.metal);shape('box',x,y+5.6,z,1.9,.16,.22,C.darkMetal);shape('cone',x,y+6.62,z,.55,1.3,.55,C.red);}if(o.site==='fire'){if(!tryDrawGLB('campfire',T(x,y,z,1,1,1,0))){for(let j=0;j<8;j++){let a=j*Math.PI/4;shape('sphere',x+Math.cos(a)*.56,y+.12,z+Math.sin(a)*.56,.33,.22,.33,C.stone)}for(let j=0;j<3;j++){let a=j*2.1;shape('cyl',x+Math.sin(a)*.11,y+.18,z+Math.cos(a)*.09,.15,.95,.15,C.wood,a+Math.PI/2)}}if(state.fireFuel>0)for(let j=0;j<3;j++)shape('cone',x+Math.sin(j*2)*.09,y+.55+Math.sin(t*9+j)*.10,z+Math.cos(j*2)*.09,.33,.90,.33,j%2?C.fire:C.gold)}
 if(o.site==='shelter'&&!tryDrawGLB('shelter',T(x,y,z,1,1,1,0))){for(let side of [-1,1]){for(let dz of [-.75,.75])shape('cyl',x+side*.95,y+1.18,z+dz,.12,2.3,.12,C.wood)}shape('box',x,y+2.29,z,2.3,.19,2.1,C.paleWood);shape('box',x,y+2.45,z-1.01,2.7,.22,.25,C.palm);shape('box',x,y+2.47,z+1.01,2.7,.22,.25,C.palm);for(let k=0;k<4;k++)shape('box',x-.87+k*.59,y+2.48,z, .55,.08,2.23,k%2?C.palm2:C.palm);shape('box',x,y+.19,z,2.3,.18,1.9,C.wood)}
 if(o.site==='filter'&&!tryDrawGLB('filter',T(x,y,z,1,1,1,0))){for(let j of [-1,1])shape('cyl',x+j*.45,y+1.10,z,.10,2.2,.10,C.wood);shape('box',x,y+2.14,z,1.20,.10,.24,C.wood);shape('cone',x,y+1.30,z,1.14,1.42,1.13,C.palm);shape('cyl',x,y+.46,z,.80,.35,.80,C.darkMetal);shape('disc',x,y+.66,z,.65,1,.65,C.water)}
 if(o.site==='signal'&&!tryDrawGLB('signal',T(x,y,z,1,1,1,0))){for(let j of [-.65,.65])shape('cyl',x+j,y+1.25,z,.16,2.5,.16,C.wood);shape('box',x,y+2.42,z,1.65,.20,1.3,C.plank);shape('cyl',x,y+3.67,z,.08,2.40,.08,C.metal);shape('box',x,y+4.75,z,.16,.07,1.80,C.metal);shape('cone',x,y+2.81,z,.52,.76,.52,C.gold)} } }
@@ -543,9 +559,12 @@
 // Camera line-of-sight: pull in before static obstacles; never burrow through ground.
 for(let t=.30;t<=1;t+=.07){let p=[mix(focus[0],desired[0],t),mix(focus[1],desired[1],t),mix(focus[2],desired[2],t)];let occluded=p[1]<H(p[0],p[2])+.72;for(let tree of staticTrees)if(Math.hypot(p[0]-tree.x,p[2]-tree.z)<tree.r&&p[1]<H(tree.x,tree.z)+3.3)occluded=true;if(occluded){let k=Math.max(.23,t-.14);desired=[mix(focus[0],desired[0],k),mix(focus[1],desired[1],k),mix(focus[2],desired[2],k)];break}}
 desired[1]=Math.max(desired[1],H(desired[0],desired[2])+.75,-.1);let sm=clamp(dt*8,0,1);for(let j=0;j<3;j++)cameraPos[j]=mix(cameraPos[j],desired[j],sm);eye=cameraPos.slice();VP=mul(perspective((shoulder?62:68)*Math.PI/180,Math.max(.1,$('world').clientWidth/$('world').clientHeight),.09,190),lookAt(eye,focus));inverseVP=inverse(VP)}
-function campComfort(){let score=0;if(state.built.shelter===3)score+=2;if(state.built.storage===3)score+=1;if(state.built.filter===3)score+=1;if(state.built.rainCollector===3)score+=1;if(state.built.fire===3&&state.fireFuel>0)score+=2;return score}
-function processSurvivalDay(){if(state.day<=state.lastSurvivalRewardDay)return;const healthy=state.hp>=45&&state.water>=20&&state.food>=20;if(healthy){state.survivalStreak=(state.survivalStreak||0)+1;const xp=Math.min(55,12+state.survivalStreak*4);awardXP(xp,'Přežitý den · série '+state.survivalStreak);state.morale=clamp(state.morale+8,0,100);state.journal.push('Den '+state.day+': Přežil jsem další den. Série přežití: '+state.survivalStreak+'.');}else{state.survivalStreak=0;state.morale=clamp(state.morale-8,0,100);}state.lastSurvivalRewardDay=state.day;changed=true}
-function gameplay(dt){if(!started||pane||mini)return;state.seconds+=dt;updateNPCs(dt);updateExploration();state.clock+=dt/120;if(Math.floor(state.seconds/5)!==Math.floor((state.seconds-dt)/5))changed=true; if(state.clock>=24){state.clock-=24;state.day++;changed=true}processSurvivalDay();if(state.story.stormUntil&&state.seconds<state.story.stormUntil)state.weather='storm';else if(state.day%5===0&&state.clock>10&&state.clock<15)state.weather='heat';else if(state.day%4===0&&state.clock>5&&state.clock<9)state.weather='fog';else if(state.day%3===0&&state.clock>13&&state.clock<17)state.weather='rain';else state.weather='clear';if(state.built.fire===3&&state.fireFuel>0)state.fireFuel=clamp(state.fireFuel-dt*(state.weather==='storm'?.11:.045),0,100);if(state.built.rainCollector===3&&(state.weather==='rain'||state.weather==='storm'))state.rainWater=clamp(state.rainWater+dt*(state.weather==='storm'?.34:.20),0,100);
+function siteDurability(site){return clamp(Number(state.durability?.[site]??100),0,100)}
+function campComfort(){let score=0;if(state.built.shelter===3&&siteDurability('shelter')>=40)score+=2;if(state.built.storage===3&&siteDurability('storage')>=40)score+=1;if(state.built.filter===3&&siteDurability('filter')>=40)score+=1;if(state.built.rainCollector===3&&siteDurability('rainCollector')>=40)score+=1;if(state.built.dryRack===3&&siteDurability('dryRack')>=40)score+=1;if(state.built.fire===3&&state.fireFuel>0)score+=2;return score}
+function processStormDamage(){if(state.weather!=='storm'||state.stormDamageDay===state.day)return;state.stormDamageDay=state.day;const sites=['shelter','storage','filter','rainCollector','trapLine','dryRack'].filter(id=>state.built[id]===3);if(!sites.length)return;const site=sites[(state.day+state.deathCount)%sites.length];const dmg=18+(state.day*7)%17;state.durability[site]=clamp(siteDurability(site)-dmg,0,100);state.fireFuel=clamp(state.fireFuel-18,0,100);writeStory('Bouře poškodila '+BUILD[site].name.toLowerCase()+' na '+Math.round(state.durability[site])+' % odolnosti.');flash('⛈ Bouře poškodila '+BUILD[site].name+' · '+Math.round(state.durability[site])+' %');changed=true}
+function processCampRaid(){if(state.day<=state.campRaidDay)return;state.campRaidDay=state.day;if(state.day<3||state.day%2!==0)return;const defense=(state.built.trapLine===3&&siteDurability('trapLine')>=40?1:0)+(state.built.fire===3&&state.fireFuel>20?1:0)+(state.built.storage===3&&siteDurability('storage')>=40?1:0);if(defense>=2){state.morale=clamp(state.morale+2,0,100);writeStory('Noční šramot kolem tábora utichl. Oheň a pasti odradily zvířata.');return;}const targets=['smokedMeat','meat','fish','coconut','trailSnack'];let stolen=null,from=null;for(const id of targets){if((state.storage?.[id]||0)>0){state.storage[id]--;stolen=id;from='skladu';break}if((state.bag?.[id]||0)>0){state.bag[id]--;stolen=id;from='batohu';break}}if(stolen){state.morale=clamp(state.morale-7,0,100);if(state.built.storage===3)state.durability.storage=clamp(siteDurability('storage')-10,0,100);writeStory('V noci se do tábora dostala zvěř a odnesla '+ITEM[stolen][1].toLowerCase()+' z '+from+'.');flash('🌙 Noční nájezd: ztraceno '+ITEM[stolen][1]);changed=true}}
+function processSurvivalDay(){if(state.day<=state.lastSurvivalRewardDay)return;const healthy=state.hp>=45&&state.water>=20&&state.food>=20;if(healthy){state.survivalStreak=(state.survivalStreak||0)+1;const xp=Math.min(55,12+state.survivalStreak*4);awardXP(xp,'Přežitý den · série '+state.survivalStreak);state.morale=clamp(state.morale+8,0,100);state.journal.push('Den '+state.day+': Přežil jsem další den. Série přežití: '+state.survivalStreak+'.');}else{state.survivalStreak=0;state.morale=clamp(state.morale-8,0,100);}state.lastSurvivalRewardDay=state.day;processCampRaid();changed=true}
+function gameplay(dt){if(!started||pane||mini)return;state.seconds+=dt;updateNPCs(dt);updateExploration();state.clock+=dt/120;if(Math.floor(state.seconds/5)!==Math.floor((state.seconds-dt)/5))changed=true; if(state.clock>=24){state.clock-=24;state.day++;changed=true}processSurvivalDay();if(state.story.stormUntil&&state.seconds<state.story.stormUntil)state.weather='storm';else if(state.day%5===0&&state.clock>10&&state.clock<15)state.weather='heat';else if(state.day%4===0&&state.clock>5&&state.clock<9)state.weather='fog';else if(state.day%3===0&&state.clock>13&&state.clock<17)state.weather='rain';else state.weather='clear';processStormDamage();if((state.bag.meat||0)>0&&!state.meatSpoilAt)state.meatSpoilAt=state.seconds+240;if((state.bag.meat||0)>0&&state.meatSpoilAt&&state.seconds>=state.meatSpoilAt){const spoiled=state.bag.meat;state.bag.meat=0;state.bag.spoiledMeat=(state.bag.spoiledMeat||0)+spoiled;state.meatSpoilAt=0;flash('🤢 Syrové maso se zkazilo.');writeStory('Maso bez zpracování nevydrželo tropické vedro.');}if(state.built.fire===3&&state.fireFuel>0)state.fireFuel=clamp(state.fireFuel-dt*(state.weather==='storm'?.11:.045),0,100);if(state.built.rainCollector===3&&(state.weather==='rain'||state.weather==='storm'))state.rainWater=clamp(state.rainWater+dt*(state.weather==='storm'?.34:.20),0,100);
 let forward=(keys.has('w')||keys.has('ArrowUp')?1:0)-(keys.has('s')||keys.has('ArrowDown')?1:0)-joy.y;let strafe=(keys.has('d')||keys.has('ArrowRight')?1:0)-(keys.has('a')||keys.has('ArrowLeft')?1:0)+joy.x;let dx=strafe*Math.cos(cameraYaw)-forward*Math.sin(cameraYaw),dz=-strafe*Math.sin(cameraYaw)-forward*Math.cos(cameraYaw);
 if(Math.hypot(dx,dz)>.09){autogoal=null;hero.goal=null;moveHero(dx,dz,dt,running||keys.has('Shift'))}else if(autogoal){let dd=d2(hero,autogoal);if(dd<Math.max(1.15,autogoal.r+.80)){let goal=autogoal;autogoal=null;if(goal.o)beginInteraction(goal.o);hero.walk=mix(hero.walk,0,dt*9)}else {let stepX=(autogoal.x-hero.x)/dd,stepZ=(autogoal.z-hero.z)/dd;let moved=moveHero(stepX,stepZ,dt,true);if(!moved&&hero.walk<.13)autogoal=null}}else{hero.walk=mix(hero.walk,0,clamp(dt*6,0,1));state.energy=clamp(state.energy+dt*(state.built.shelter===3?.80:.48),0,100)}
 if(state.food<=0||state.water<=0){state.hp=clamp(state.hp-dt*.44,0,100);if(state.hp<=0)handleDeath(state.water<=0?'dehydratace':'hlad')}